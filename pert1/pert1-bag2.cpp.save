#include <iostream>
#include <cstring>
using namespace std;

class mobil
{
private :
      int roda, pintu;
      char warna [100];
public :
      void data(int jum_roda, int jum_pintu, const char* warna_mobil);
      void info();
};

int main()
{
      //mendeklarasikan objek
      mobil mitsubishi;
      mobil toyota;

      cout<<"Mobil Mitsubishi : \n";
      cout<<"-------------------"<<endl;
      lamborghini.info(); //memanggil fungsi info();

      cout<<endl;
      cout<<"Mobil TOYOTA :"<<endl;
      cout<<"-----------"<<endl;
      bmw.data(4,4,"Silver"); //memanggil fungsi data()
      bmw.info(); //memanggil fungsi info();

      return 0;
}

void Mobil::data(int jum_roda, int jum_pintu, const char* warna_mobil)
{
      roda  = jum_roda;
      pintu = jum_pintu;
      strcpy(warna, warna_mobil);
}

void Mobil::info()
{
      cout<<"Jumlah Roda  : "<<roda<<endl;
      cout<<"Jumlah Pintu : "<<pintu<<endl;
      cout<<"Warna        : "<<warna<<endl;
}
